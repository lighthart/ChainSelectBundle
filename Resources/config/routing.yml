lighthart_selectize_entity:
    path:     /{class}/{method}/{criteria}
    defaults: { _controller: LighthartSelectizeBundle:Query:json, 'criteria': null, 'method': null }
    requirements:
        class: \w+
        criteria: ((\w+_\d+(,\d+)*)(__(\w+_\d+(,\d+)))*)*
        method: \w+
